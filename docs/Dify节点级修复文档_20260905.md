# Dify 节点与配置详细修复文档（2026-09-05）

> 性质：修复方案文档（未修改任何 YAML）
> 依据当前导出：
> - `C:\Users\34862\Downloads\dify\通信工程智能化总控（PROD）V1.0！！.yml`（24 节点）
> - `C:\Users\34862\Downloads\dify\施工指令工具（PROD）.yml`
> - `C:\Users\34862\Downloads\dify\纤芯分配工具（PROD）.yml`
> - `C:\Users\34862\Downloads\dify\BOM工具（PROD）.yml`
>
> 后端已部署现状（Dify 修复必须消费）：
> - `/agent/data-pipeline`：`review.status/executed/reason/error`、`stage_results`；
> - `/agent/bom`：每行含 `quantity/unit/material_code/source_object_ids`（中文旧字段保留）。

## 〇、配置定位速查（精确到 Body/Headers/variables/outputs）

> 以下“当前值”均取自 2026-09-05 导出文件；“改为”表示在 Dify 节点编辑面板中修改的配置项。

| 文件 | 节点 ID/标题 | 配置对象 | 当前值 | 改为/新增 |
|---|---|---|---|---|
| 总控 | 1786366895071 调用BOM_DEV_API | URL | `http://118.31.127.213:8001/agent/bom` | 保持 |
| 总控 | 同上 | Headers | `Content-Type:application/json` | 保持（不加 Authorization） |
| 总控 | 同上 | Body | `{"type":"json","data":[{"key":"","type":"text","value":"{\"project_id\":\"{{#1785836509606.project_id#}}\"}"}]}` | 保持 project_id 模式；若需传 engineering_data 再改为引用 `1786367110630.body` |
| 总控 | 1785834889406 HTTP 请求 | Headers | 空字符串 | 保持（后端无需鉴权） |
| 总控 | 1785834889406 | Body | multipart：file=1785836300668.first_record；excel_limit=0、pdf_chars=0、include_tables=false、compact=true | 保持 |
| 总控 | 1786367437101 解析BOM_DEV_API返回 | variables | `body <- 1786366895071.body` | 保持 |
| 总控 | 1786367437101 | outputs | api_success/api_status/api_error/bom_markdown/bom_result_json/... | 保持（作为信号源） |
| 总控 | 1786023921854 解析黄福林BOM结果 | variables | 仅 `bom_result_json <- 1786367437101.bom_result_json` | **新增** `api_success <- 1786367437101.api_success`、`api_error <- 1786367437101.api_error` |
| 总控 | 1785836509606 解析真实接口返回 | outputs | api_success/…/issues_text 等 | **新增** `review_state`、`review_reason`、`review_error` |
| 总控 | 1785836928806 真实接口结果判断 | if-else 条件 | `api_success == true` | 改为 `api_success == true AND review_state == "executed"` |
| 总控 | 1786031004832 全流程交付报告格式化 | variables | 无 construction pending 输入 | **新增** `environment_pending_count <- 1786378171088.environment_pending_count`、`manual_package_count <- 1786378171088.manual_package_count`、`review_state <- 1785836509606.review_state` |
| 总控 | 1786031004832 | outputs | final_report/overall_status/pending_count | **新增** `result_type`、`stage_results_json` |
| 总控 | 1785573827916 交付流程状态输出 | End outputs | 第一项 `delivery_status = 1786031004832.final_report` | **新增** `overall_status <- 1786031004832.overall_status`、`result_type <- 1786031004832.result_type`、`stage_results_json <- 1786031004832.stage_results_json` |
| 施工 | 1785839947624 代码执行 | outputs schema | 无 environment_pending_count/manual_package_count | **新增** `environment_pending_count(number)`、`manual_package_count(number)`、`pending_review_items_json(string)` |
| 施工 | 1785839947624 | 代码 return | 未返回上述三个字段 | return 字典补 `environment_pending_count/manual_package_count/pending_review_items_json` |
| 施工 | 1785841073663 输出 | End outputs | 无上述字段 | **新增** `environment_pending_count <- 1785839947624.environment_pending_count`、`manual_package_count <- 1785839947624.manual_package_count`、`pending_review_items_json <- 1785839947624.pending_review_items_json` |
| 纤芯 | 1785820320131 纤芯分配引擎 | outputs schema | fiber_status/fiber_count/splice_count/conflict_count/fiber_result_json | **新增** `cable_allocation_row_count(number)`、`splice_location_count(number)`、`splice_core_count(number)`、`retrieval_status(string)` |
| 纤芯 | 1785820356858 输出 | End outputs | 无新字段 | **新增** 4 个 output：`cable_allocation_row_count <- 1785820320131.cable_allocation_row_count`、`splice_location_count <- 1785820320131.splice_location_count`、`splice_core_count <- 1785820320131.splice_core_count`、`retrieval_status <- 1785820320131.retrieval_status` |
| 纤芯 | 1785820320131 | variables | `kb_result <- 1785820309371.result` | 修复代码不使用 kb_result 计算；若保留检索则新增 `retrieval_status` 输出 |
| 总控 | 1786378158503/1786378171088 工具节点 | tool_parameters | engineering_data=1785836509606.engineering_data_text；required_cores=4；bom_result=1786367437101.bom_result_json | 工具应用修复发布后，重绑正式版本；参数映射保持不变 |


---

## 一、总控（PROD V1.0！！）节点级修复

### 1.1 节点 1786367437101「解析BOM_DEV_API返回」

**现状（已核验代码）**
- 输出：`api_success / api_status / api_error / bom_markdown / bom_result_json / workflow_run_id / task_id`；
- `api_success = root.success`，解析失败时 `api_status=parse_failed`。

**需改动**
- 该节点本身结构基本正确，**保持输出不变**；
- 作为"业务失败信号源"接入下游（见 1.2），不得只把 `bom_result_json` 交给下游。

**配置/连线修复**
- 新增连线或变量引用：把 `api_success`、`api_error` 暴露给 `1786023921854 解析黄福林BOM结果`。

### 1.2 节点 1786023921854「解析黄福林BOM结果」

**现状（已核验代码）**
- `main(bom_result_json)`，只要 JSON 可解析即 `bom_success=True`；
- 输入 variables 只有 `1786367437101.bom_result_json`。

**修复方式**
1. Code 节点 variables 增加两个入参：
   - `api_success` ← `1786367437101.api_success`
   - `api_error` ← `1786367437101.api_error`
2. `main` 签名改为 `main(bom_result_json="", api_success=None, api_error="")`；
3. 逻辑改为：
```python
if api_success is False:
    return {"bom_success": False, "bom_error": str(api_error or "BOM API 失败"),
            "final_bom": [], "standard_bom_table": [], "need_confirm_list": [],
            "bom_count": 0, "confirm_count": 0}
text = bom_result_json.strip()
if not text or text == "{}":
    return {"bom_success": False, "bom_error": "BOM结果为空", ...}
data = json.loads(text)  # 失败同样返回 bom_success=False
items = data.get("final_bom") or data.get("standard_bom_table") or []
if not items:
    return {"bom_success": False, "bom_error": "BOM无物料行", ...}
```

**验收**：业务失败、500、非 JSON、空对象四种输入，`bom_success` 均为 False。

### 1.3 节点 1785836509606「解析真实接口返回」（RUN-04）

**现状**
- `data.get("review")` 非 dict 时用 `{}`，`issues=[]`、计数全 0，`issues_text="未发现审查问题或警告"`；
- 仅用 `data.success` 作为 `api_success`。

**修复方式**
1. 入参不变（HTTP body）；读取后端新字段：
```python
review = data.get("review") if isinstance(data.get("review"), dict) else None
review_status = (review or {}).get("status")
if review is None:
    review_status = "missing"
elif review_status in ("skipped", "error", "not_run"):
    ...
```
2. 分支规则：
   - `review.status == "skipped"`：输出 `review_state="skipped"`、reason 透传，业务侧不得把 0 规则当"无风险"；
   - `review.status == "error"` 或 `data.success is False`：`api_success=False`，`error_message` 输出 review.error；
   - `issues` 缺失但 status=executed：契约错误。
3. 输出 variables 增加 `review_state`、`review_reason`，供门禁与最终格式化使用。

### 1.4 节点 1786031004832「全流程交付报告格式化」（RUN-02/CON-01）

**现状**
- `pending_count = confirm_count + conflict_count + unknown_count`；
- 只判 `fiber_status=="失败"` 或 `construction_status=="失败"`；
- 不接收 `environment_pending_count / manual_package_count / pending_review_items`。

**修复方式**
1. Code 节点 variables 增加：
   - `construction_status` ← 1786378171088（已有）；
   - `environment_pending_count` ← 1786378171088（施工工具修复后输出）；
   - `manual_package_count` ← 1786378171088；
   - `pending_review_items_json` ← 1786241934098（可选）；
   - `review_state` ← 1785836509606。
2. `main` 终态判定改为机器枚举：
```python
if api/bom/fiber/construction 任一解析失败或 review_state=="error":
    overall = "BLOCKED"
elif review_state=="skipped" and 必需审查未执行:
    overall = "BLOCKED"
elif failed_rules > 0:
    overall = "CONDITIONAL_PASS_ESTIMATED"   # 仅预估
elif confirm_count>0 or conflict_count>0 or unknown_count>0 \
     or environment_pending_count>0 or manual_package_count>0:
    overall = "CONDITIONAL_PASS"
else:
    overall = "PASS"
```
3. 报告中文展示与 `overall_status` 机器字段分离：`final_report` 只做展示，`overall_status` 只做机器判定。

**验收**：施工状态=待确认且 confirm/conflict/unknown 全 0 → 不得 PASS；fiber_status="解析失败" → 不得 PASS。

### 1.5 节点 1785573827916「交付流程状态输出」（RUN-03）

**现状**
- outputs 第一项 `delivery_status` = `1786031004832.final_report`（报告全文）。

**修复方式**
- outputs 改为：
  1. `delivery_status`（展示用，仍取 final_report，名称改为 `final_report`）；
  2. `overall_status` ← `1786031004832.overall_status`；
  3. `result_type` ← `1786031004832.result_type`（若新增）；
  4. `stage_results_json` ← `1786031004832.stage_results_json`；
  5. 原有 `bom_* / fiber_* / construction_*` 计数输出保留。

**验收**：Web/API 输出有机器状态，不再用报告全文顶替状态。

### 1.6 节点 1786366895071「调用BOM_DEV_API」

**现状**
- HTTP URL = `http://118.31.127.213:8001/agent/bom`，body 传 `project_id`（当前导出）——方向正确。

**需配置确认**
- 若准备节点 1786367110630 未实际被 HTTP body 引用，删除该悬空节点或让 body 引用其输出，避免维护两套请求体；
- headers 保持 `Content-Type: application/json`，不携带密钥；
- 超时建议 read/write 保持 ≥120s。

### 1.7 总控其它节点

| 节点 | 操作 |
|---|---|
| 1785834889406 HTTP 请求（审查） | 已直连 `/agent/data-pipeline`，无改动；新增日志/request_id 对账可选 |
| 1785836928806 真实接口结果判断 | 条件改为 `api_success AND review_state=="executed"`（若 review 跳过/错误必须走失败分支） |
| 1786241934098 解析施工指令结构化结果 | 保留；若上游损坏需输出 `construction_parse_success=false` 阻断 |
| 1786378158503 / 1786378171088 工具节点 | 修复对应工具应用并发布后，重绑正式版本（见第五节） |

---

## 二、施工指令工具（PROD）节点级修复

文件：`施工指令工具（PROD）.yml`，主代码节点 `1785839947624 代码执行`，知识检索节点 `1785839847556`。

### 2.1 对象完整性（CON-02）——object_records 构建段

**现状**
- 只遍历 `boites`、`cables` 构造 `object_records`；`ptechs` 读取后未使用；site/infrastructure 未处理。

**修复示意**
```python
object_records = []
for source_kind, items in (("boite", boites), ("cable", cables),
                           ("ptech", ptechs), ("site", objects.get("site") or []),
                           ("infrastructure", objects.get("infrastructure") or [])):
    for item in items:
        if not isinstance(item, dict): continue
        obj_type = normalize_type(item.get("type"), source_kind)
        if source_kind not in ("boite", "cable") and obj_type == "UNKNOWN":
            # 保留为未知对象，禁止静默丢弃
            ...
        object_records.append({... "source_id": item.get("id") or f"{source_kind}:{item.get('code','')}", ...})
```

**验收**：仅 PTECH 输入必须产生工作包或"未覆盖对象"记录。

### 2.2 材料字段与对象归属（CON-03/CON-04）——material_for_codes

**现状**
- `related = 对应设计对象/design_object/object_code`，`code in related` 子串匹配；
- `qty = item.get("数量") if "数量" in item else item.get("quantity")`；
- 返回 `list(grouped.values())[:30]`。

**修复示意（读取 quantity/unit + source_object_ids 精确匹配 + 不截断）**
```python
def material_for_codes(codes, source_ids):
    matched = []
    for item in bom_items:
        if not isinstance(item, dict): continue
        item_ids = item.get("source_object_ids") or []
        codes_ok = item.get("source_object_codes") or []
        if item_ids and source_ids:
            hit = bool(set(item_ids) & set(source_ids))
        else:
            hit = any(str(c) in [str(x) for x in item_ids] for c in codes)
        if not hit:
            continue
        qty = item.get("quantity")
        if qty is None:
            qty = item.get("最终数量")          # 旧后端兼容
        unit = item.get("unit") or item.get("单位")
        code = item.get("material_code") or item.get("物料编码")
        name = item.get("material_name") or item.get("物料名称")
        matched.append({... "数量": qty, "单位": unit, ...})
    # 聚合去重后完整返回；displayed 单独给展示层
    return grouped, len(grouped)
```
- 输出端需要同时提供 `full_materials`（完整）与 `displayed_materials[:30]`、`total/displayed`；
- `matched_fibers[:30]` 同样只用于展示字段，机器输出保留完整数组。

**验收**：C-1/C-10 不串料；30 类以上材料完整 JSON/导出不丢；AUD-12 材料行无空数量/单位。

### 2.3 证据与规程引用（CON-05）

**现状**
- `PCP_OFFICIAL_EVIDENCE` 在已知对象分支无条件 `extend`；
- `procedure_ref["已验证参数"]` 固定写入 Fiber-Fiber 0.1/0.2dB 等。

**修复方式**
1. `PCP_OFFICIAL_EVIDENCE` 改为带版本库标记的静态条目：
```python
{"category": ..., "evidence_type": "versioned_static",
 "source": "Operational Procedure PCP Installation v3.9",
 "source_version": "v3.9", "section": "Table 5 / Page 43",
 "verified_at": "...", "note": "静态版本已核验；不等于本次动态检索可用"}
```
2. `procedure_ref` 增加 `evidence_origin`、`retrieval_status`（hit/miss/unavailable）；
3. 只有 `retrieval_status=="hit"` 时才允许"本次检索命中"表述；
4. KB 未命中时 `已验证参数` 不允许作为"已核验本次"输出，放入"待人工确认-静态参数待复核"。

**验收**：KB 未命中样例仍可输出静态摘录，但检索状态=miss、无"已验证"误导。

### 2.4 输入损坏处理（CON-06）——parse 与 fiber_pending

**现状**
- `parse()` 失败返回 default；`fiber_pending = bool(conflicts or fiber_status=="待确认")`，"失败/解析失败"不计入。

**修复**
```python
fiber_pending = fiber_status in ("失败", "FAILED", "解析失败") or bool(fiber_conflicts) or fiber_status == "待确认"
```
- `fiber_result` 解析失败/空时置 `fiber_pending=True` 并写 pending reason；
- 只有显式 `NOT_APPLICABLE` 才允许无 Fiber 继续。

### 2.5 输出端/End 节点配置（CON-01 工具侧）

**配置修复**
- 主代码节点 outputs 已含 environment_pending_count/manual_package_count/pending_review_items（在 construction_result 内）；
- End 节点 `1785841073663 输出` 的 outputs 需显式暴露：
  - `construction_status`
  - `environment_pending_count`
  - `manual_package_count`
  - `pending_review_items_json`
  - `material_rows_total`（便于总控对账）
- 发布后，总控工具节点 1786378171088 才会看到新变量。

### 2.6 知识检索配置（RAG-01）

**现状**
- 检索节点 `1785839847556` 的 query 用工程数据原文；`procedure_hit` 靠关键词。

**修复**
- 检索节点增加错误处理分支（LLM/检索失败 → `retrieval_unavailable=true`），输出 `retrieval_status`；
- 知识检索结果只进"增强字段"，不进确定性数量计算；
- 无检索结果时不得触发 exception 导致整条工作流失败。

---

## 三、纤芯分配工具（PROD）节点级修复

文件：`纤芯分配工具（PROD）.yml`，主代码节点 `1785820320131 纤芯分配引擎`，知识检索 `1785820309371`，End `1785820356858`。

### 3.1 占用一致性（FIB-01）

**修复示意**
```python
# 逐芯表 + 聚合占用双源校验
per_core = {code: existing...}
for cable in cables:
    agg = to_int(cable.get("nb_fibre_util"), -1)
    if agg >= 0 and per_core:
        if len(per_core[code]) != agg:
            conflicts.append({"冲突类型": "聚合与逐芯占用不一致", ...})
    elif agg > 0 and not per_core:
        conflicts.append({"冲突类型": "仅聚合占用无芯号，禁止猜测空闲", ...})
    if agg >= capacity:
        conflicts.append({"冲突类型": "满占用/超容量，禁止新增", ...})
```

### 3.2 连续选芯（FIB-02）

**配置/决策**
- 在 business_params/输入增加 `allocation_policy`（`contiguous`/`smallest_free`）；
- 实现 `contiguous` 时按 tube→core 找连续空闲区间；占用 2、需求 2 时应选 3、4；
- 若团队不保证连续性，则把文档与输出字段统一为 `smallest_free`，不宣称连续。

### 3.3 splice_count 口径（FIB-04）

**输出修复**
- `compact_result` 增加：
  - `cable_allocation_row_count`（原 splice_count 语义，保持兼容）；
  - `splice_location_count`（真实接头点行数，无数据为 0/空）；
  - `splice_core_count`（逐芯熔接次数，需逐芯表）；
- End 节点 outputs 同步暴露新字段；旧 `splice_count` 仅保留兼容名并注明口径。

### 3.4 输入校验（FIB-05）

- `required_cores`：`<=0` 或非数字 → `fiber_status="失败"`，不 `max(1,…)`；
- 空 `cables`：显式 `NOT_APPLICABLE`（有说明）或 `FAILED`（缺必要数据），不得默认"正常"；
- capacity/modulo 非整除或缺失 → 冲突/待确认。

### 3.5 知识检索（RAG-01，纤芯侧）

**现状**：检索节点在引擎之前，但 `kb_result` 参数从未被使用。
**修复**：引擎移除 `kb_result` 依赖；若保留检索节点，则输出 `retrieval_status/retrieval_unavailable` 到报告增强字段，且计算不等待检索成功。

### 3.6 End 节点 `1785820356858`

- outputs 增补：`cable_allocation_row_count/splice_location_count/splice_core_count/retrieval_status`；
- 输出字段名与总控解析节点 `1786028387147` 读取保持一致。

---

## 四、独立 BOM 工具（PROD）处理

文件：`BOM工具（PROD）.yml`

| 项 | 建议 |
|---|---|
| 主链 | 当前总控已直连后端 /agent/bom，独立 BOM 工具不参与主链 |
| 配置 | 方案 A：Dify 控制台停用/移出正式入口；方案 B：若保留，则代码节点改调后端 /agent/bom（含 project_id 或 engineering_data）并透传 quantity/unit |
| 文档 | BOM-R03/R06 保留"待官方确认"标记，禁止自动匹配为正式数量 |

---

## 五、版本与发布配置操作

1. 施工工具：修改 `1785839947624` 代码 → 更新 End `1785841073663` outputs → 发布应用；
2. 纤芯工具：修改 `1785820320131` 代码 → 更新 End `1785820356858` outputs → 发布应用；
3. 总控：按 1.1~1.5 修改节点 variables/代码/End outputs → 发布；
4. 重绑工具节点：把 `1786378158503`、`1786378171088` 指向刚发布的正式版（替换 PROD-CANDIDATE-20260810 的绑定）；
5. 验证四路径：正向、停止整改、继续预估、仅审查；并用实际 API JSON 跑施工确认材料数量/单位保留。

## 六、修复顺序

1. 施工工具 2.1~2.5（AUD-12 依赖，优先）；
2. 总控 1.1~1.5（门禁与失败传播）；
3. 纤芯工具 3.1~3.5（需先定 FIB-02/FIB-03 策略）；
4. 独立 BOM 工具归档/换绑（BOM-01）。

> 本文档为实施蓝本；每个节点修改后都需在 Dify 中"发布"，API 调用的是已发布版本而非草稿。

## 决策记录（2026-09-05）

- BOM 工具只用我们后端 `/agent/bom`（负责人已确认）：Dify 独立 BOM 工具不再承担计算，独立 BOM 工具 YAML 归档/停用；BOM-R03/R06 不作为正式能力，总控保持直连后端。
