# Dify 修复清单与修复方式（2026-09-05）

> 编制：主脑（Codex）　|　性质：修复方案文档（不执行修改）
> 依据文件：
> - `C:\Users\34862\Downloads\dify\通信工程智能化总控（PROD）V1.0！！.yml`（2026-09-05 当前导出）
> - `C:\Users\34862\Downloads\dify\施工指令工具（PROD）.yml`
> - `C:\Users\34862\Downloads\dify\纤芯分配工具（PROD）.yml`
> - `C:\Users\34862\Downloads\dify\BOM工具（PROD）.yml`
>
> 重要前提：后端已于 2026-09-05 部署：
> - `/agent/data-pipeline` 返回 `review.status/executed/reason/error` 与 `stage_results`；
> - `/agent/bom` 每行已返回 `quantity/unit/material_code/source_object_ids`（中文旧字段保留）。
> Dify 修复必须消费这些新字段，否则 AUD-12/RUN-04 仍无法闭环。

---

## 一、总控（通信工程智能化总控 PROD V1.0）修复项

| 编号 | 节点/位置 | 问题（已核验） | 修复方式 | 验收 |
|---|---|---|---|---|
| RUN-01 | `1786367437101 解析BOM_DEV_API返回` → `1786023921854 解析黄福林BOM结果` | BOM API `success=false`/非 JSON 经二次解析仍可能 `bom_success=true` | ①把 `api_success/api_status/api_error` 传入 `1786023921854`；②合法 JSON ≠ 业务成功：`api_success=false`、空对象、无 bom_items 均置 `bom_success=false`；③失败不进入正式生成分支 | 接口业务失败/500/非 JSON/空对象四种输入最终均不得 PASS |
| RUN-02 | `1786031004832 全流程交付报告格式化` | 只比较 `fiber_status=="失败"` 与 `construction_status=="失败"`；`解析失败/FAILED/待确认/ERROR` 有误放行路径；`pending_count` 只含 confirm/conflict/unknown | ①统一机器枚举（FAILED/BLOCKED/CONDITIONAL_PASS/NOT_APPLICABLE/PASS），中文仅展示；②终态由阶段结果+ERROR+人工确认项共同计算；③把 `construction_status`、`environment_pending_count`、`manual_package_count` 纳入判定 | 失败模块必 FAILED；ERROR 必阻断；待确认至多 CONDITIONAL；正向样例 PASS |
| RUN-03 | `1785573827916 交付流程状态输出` 与 BOM→Fiber→Construction 连线 | `delivery_status` 绑定报告全文而非机器状态；后段无统一阶段 Gate | 结束节点分别输出 `overall_status`、`result_type`、`stage_results`、`final_report`；在生成正式成果前加统一 Gate | API 失败/资源冲突不形成正式包；Web 与 DSL 机器状态一致 |
| RUN-04 | `1785836509606 解析真实接口返回` | review 缺失/非 dict 时得到 0 检查、0 失败，`issues_text="未发现审查问题或警告"` | ①校验必需结构与计数-明细一致性；②缺失/损坏置"未执行/失败"，不等于零风险；③消费后端新增 `review.status/stage_results`；④负计数、计数不一致返回契约错误 | 缺 review、负计数、计数不一致、错误枚举均进入明确异常状态 |
| CON-01（总控侧） | `1786378171088 施工工具节点` → `1786031004832` | 施工工具返回 `待确认`（环境/人工待确认）时，总控计数为 0 仍判"全流程通过" | 总控新增入参 `construction_status`、`environment_pending_count`、`manual_package_count`、`pending_review_items`，任一待确认为 CONDITIONAL_PASS；不能只看 unknown/confirm/conflict | AUD-13 输入终态不得为"全流程通过" |

---

## 二、施工指令工具（PROD）修复项

文件：`施工指令工具（PROD）.yml`，主节点 `1785839947624 代码执行`（SHA a16983740f）。

| 编号 | 位置 | 问题（已核验） | 修复方式 | 验收 |
|---|---|---|---|---|
| CON-02 | object_records 构建段（约 YAML 396-451） | 只遍历 boites/cables；`ptechs` 读取后从未使用；仅 PTECH 输入返回 0 包且"正常" | ①boite/cable/ptech/site/infrastructure 都生成对象记录；②不支持的适用对象输出"未覆盖+原因"；③输出适用对象覆盖率与未覆盖清单 | 仅 PTECH 输入必须出现"未覆盖对象"或对应工作包，不得静默 0 包 |
| CON-03 | `material_for_codes()`（约 739-837） | 只读 `数量/quantity`；后端真实字段为 `quantity`（已部署）与中文 `最终数量`；BOM 聚合行无法命中；AUD-12 实测 2/2 空 | ①读取 `quantity`（若为旧后端则兼容 `最终数量`）；②读取 `unit/单位`；③按对象归属策略消费 `source_object_ids`（见下方归属口径） | 用真实 /agent/bom JSON 跑施工：所有材料行有物料编码+数量+单位，无"详见BOM"兜底 |
| CON-04 | `material_for_codes()` 的 `code in related`；`list(grouped.values())[:30]`；`matched_fibers[:30]` | BO-1 可误匹配 BO-10；完整包所需材料被截断 | ①用 `source_object_ids` 精确交集匹配（对象 id 取 `engineering_data` 的 `id` 或 `{kind}:{code}`）；②截断只用于展示，完整 JSON/导出不截断，输出 `total/displayed` | C-1/C-10 反例不串料；30+ 材料不被丢弃 |
| CON-05 | `PCP_OFFICIAL_EVIDENCE`（约 940-980）无条件追加；`procedure_ref.已验证参数`（约 1231-1260）固定写入 | KB 未命中时仍附 official_extract 与"已验证参数" | ①静态证据改为版本化库，输出 `evidence_origin=versioned_static+版本/条款/核验记录`；②`retrieval_status` 独立；③未核验条款进待确认，不显示"已验证参数" | KB 未命中样例：pending 原因存在且无"已验证参数"字样 |
| CON-06 | `parse()` 与 `fiber_pending`（约 19-46、214-222） | 损坏 fiber JSON → `{}` → `fiber_pending=false`；KB 命中时可返回"正常" | ①上游必需结果解析失败 → FAILED；②`fiber_status` 为"失败/解析失败"时阻断；③仅显式 N/A 分支允许无 Fiber | AUD-15（损坏 JSON）最终不产生正式工作包 |
| CON-01（工具侧） | 输出端 1869-1997 | 已计算 `environment_pending_count/manual_package_count`，但总控未接收 | 工具 end 输出直接暴露 `environment_pending_count/manual_package_count/pending_review_items`，且与 `construction_status` 同步 | 含环境/人工确认项时工具返回待确认结构化清单 |
| RAG-01（施工） | 知识检索 `1785839847556` 与 `procedure_hit`（约 234） | kb_result 只做关键词命中（pcp/table5/v3.9），无结构化检索状态 | ①输出 `retrieval_unavailable/retrieval_status`；②检索失败不得把未核验条款显示为已核验；③确定性计算不依赖检索结果 | 检索失败/为空时证据状态为 unavailable/待确认 |

**BOM 聚合行归属口径（CON-03/04 前置决策，三选一）**
- A：后端 BOM 输出对象级明细行（电杆/箱体可逐对象；光缆/钢绞线因整盘取整需说明分摊规则）；
- B：工作包保留"汇总材料+归属对象清单"，避免重复计数（推荐先做）；
- C：按对象数量/长度比例拆分，需要书面规则。

---

## 三、纤芯分配工具（PROD）修复项

文件：`纤芯分配工具（PROD）.yml`，主节点 `1785820320131 纤芯分配引擎`（SHA f35f491120）。
Dify 总控以工具节点"纤芯分配工具（PROD-CANDIDATE-20260810）"调用，required_cores=4。

| 编号 | 位置 | 问题（已核验，Dify API 实测） | 修复方式 | 验收 |
|---|---|---|---|---|
| FIB-01 | `get_existing/occupied` | 只读 `fiber_assignments`，不看聚合 `nb_fibre_util`；24 芯满占用无逐芯表时仍新增 4 芯且"正常" | ①优先逐芯表，同时校验聚合 `nb_fibre_util/capacite` 一致性；②满占用即阻断；③只有聚合数无芯号时返回"待确认"，禁止猜空闲 | AUD-07 输入不得新增分配 |
| FIB-02 | 扫描分配循环 | 芯 2 占用、需求 2 时分配 1、3（实测 `1-1-1,1-1-3`），不是连续策略 | 二选一：实现连续区间搜索（占用 2、需求 2 → 3、4），或明确"当前不保证连续性"并同步技术书 | AUD-09 按选定策略输出 |
| FIB-03 | 技术书 7.1~7.3 vs 引擎；总控 required_cores=4 | 文档宣称路径/ODF/跨段，实际仅逐缆最小可用扫描 | 决策后二选一：A. 新增后端确定性接口并替换 Dify 工具节点；B. Dify 只保留"逐段候选分配"，删除端到端/ODF 承诺（此条需同步技术书） | 文档与实现一致 |
| FIB-04 | `compact_result.splice_count` | `splice_count=len(splice_table)`=光缆行数；2 缆各 4 芯实测 fiber_count=8、splice_count=2 | 改名/拆字段：`cable_allocation_row_count`、真实 `splice_location_count`、`splice_core_count`；有真实接续记录才计算 | 光缆行数 ≠ 熔接工程量 |
| FIB-05 | 输入校验和异常分支 | required_cores=-5 被 `max(1,…)` 静默改成 1 并"正常" | ①required_cores 必须正整数，非法即 FAILED；②容量/modulo 整除校验；③空 cables 区分 NOT_APPLICABLE（无需求）与 FAILED（缺数据） | 非法/负值/损坏输入返回明确失败 |
| RAG-01（纤芯） | 知识检索 `1785820309371` → 引擎 | 引擎完全不消费 `kb_result`，检索先于计算且无降级状态 | 检索改独立增强分支，输出 `retrieval_unavailable`；确定性计算不依赖检索 | 检索失败不影响分配结果且状态可见 |

---

## 四、独立 BOM 工具（BOM-01）

文件：`BOM工具（PROD）.yml`（BOM-R03/R06 自述"待官方确认"）。

| 编号 | 问题 | 修复方式 | 验收 |
|---|---|---|---|
| BOM-01 | 该独立工具当前未被总控主链调用（主链已直连后端 /agent/bom）；公式无官方证据 | ①若不再使用：移出正式入口/归档并注明；②若继续使用：改为调用已部署后端 `/agent/bom` 或同步新规范键，公式保持"待确认"标记 | 主链与实际 BOM 实现、文档三者一致 |

---

## 五、版本发布与联调操作项

| 项 | 操作 | 验收 |
|---|---|---|
| 工具节点版本 | 纤芯/施工节点目前绑定 `PROD-CANDIDATE-20260810`（工具描述"实验版/测试版"）；修复后重新发布并改绑正式版本 | 节点记录与实际发布工具版本一致 |
| 后端新字段消费 | 总控/施工解析节点使用 `review.status/stage_results`、`quantity/unit/source_object_ids`（已部署） | 用后端真实 JSON 全链路复测 |
| 四条路径回归 | 正向、停止整改、继续预估、仅审查 | 终态机器状态与报告一致 |
| Dify 发布 | 修改后必须"发布"，API 调用的是已发布版本，不是编辑草稿 | API 结果与界面一致 |
| AUD-12 复验 | 用 TC-01 跑总控→拿真实 bom_items→跑施工 | 材料行 0 个"详见BOM/空数量" |

---

## 六、建议修复顺序（按风险）

1. 第一批（止血，P0）：总控 RUN-01/02/03/04 + CON-01 传播；
2. 第二批（正确性，P0/P1）：施工 CON-02~06、BOM 归属口径（优先方案 B）、RAG-01；
3. 第三批（决策后）：纤芯 FIB-01~05（先定 FIB-03 路线）；
4. 第四批（收口）：工具版本重绑、独立 BOM 工具归档、全链路四路径回归。

## 七、不修改范围（本轮文档）

- 本文只列清单与修复方式，未修改任何 Dify DSL；
- 后端 `/agent/bom`、`/agent/data-pipeline` 已完成部署，供 Dify 修复后直接联调；
- 111 正式稿、施工工具独立 Key、可访问样例 URL 仍未提供，涉及项需补齐后才能完整验收。
