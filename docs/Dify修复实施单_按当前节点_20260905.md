# Dify 修复实施单（按当前节点逐一对照，2026-09-05）

> 依据当前导出：`C:\Users\34862\Downloads\dify\通信工程智能化总控（PROD）V1.0！！.yml`（24 节点）
> 以及 `施工指令工具（PROD）.yml`、`纤芯分配工具（PROD）.yml`。
> 本单只列"当前实际存在且需要改"的节点；每个节点给出：当前值 → 应改为（精确到 variables/outputs/End/if-else/code）。

---

## A. 总控（当前 PROD V1.0！！.yml）逐节点修改

### A1. 节点 1786023921854「解析黄福林BOM结果」（Code）

**1) 输入 variables：当前 → 改为**

当前（只有 1 项）：
```json
[
  {"value_selector": ["1786367437101", "bom_result_json"], "value_type": "string", "variable": "bom_result_json"}
]
```
改为（新增 2 项）：
```json
[
  {"value_selector": ["1786367437101", "bom_result_json"], "value_type": "string", "variable": "bom_result_json"},
  {"value_selector": ["1786367437101", "api_success"], "value_type": "boolean", "variable": "api_success"},
  {"value_selector": ["1786367437101", "api_error"], "value_type": "string", "variable": "api_error"}
]
```

**2) 函数签名与开头：当前 → 改为**

当前：
```python
def main(bom_result_json: str = "") -> dict:
    try:
        if isinstance(bom_result_json, str):
            text = bom_result_json.strip()
            if not text:
                raise ValueError("BOM结果为空")
            data = json.loads(text)
```
改为：
```python
def main(bom_result_json: str = "", api_success=None, api_error: str = "") -> dict:
    if api_success is False:
        return {"bom_success": False, "bom_error": str(api_error or "BOM API 失败"),
                "final_bom": [], "standard_bom_table": [], "need_confirm_list": [],
                "bom_count": 0, "confirm_count": 0}
    try:
        if isinstance(bom_result_json, str):
            text = bom_result_json.strip()
            if not text:
                raise ValueError("BOM结果为空")
            data = json.loads(text)
```

**3) outputs：不变**
保持：bom_success/bom_error/final_bom/standard_bom_table/need_confirm_list/bom_count/confirm_count。

---

### A2. 节点 1785836509606「解析真实接口返回」（Code）

**1) outputs：当前 → 新增 3 项**

当前 outputs 键：api_success, engineering_data, engineering_data_text, error_message, failed_rules, issues_text, layer_count, object_count, passed_rules, project_id, project_name, project_type, total_rules, warning_rules

新增：
```json
{"review_state": {"children": null, "type": "string"}},
{"review_reason": {"children": null, "type": "string"}},
{"review_error": {"children": null, "type": "string"}}
```

**2) 代码修改点**

当前：
```python
review = (data.get("review") if isinstance(data.get("review"), dict) else {})
issues = (review.get("issues") if isinstance(review.get("issues"), list) else [])
...
if issue_lines:
    issues_text = ...
else:
    issues_text = "未发现审查问题或警告。"
```
改为：
```python
review = data.get("review") if isinstance(data.get("review"), dict) else None
review_state = "executed"
review_reason = ""
review_error = ""
if review is None:
    review_state = "missing"
    review_reason = "接口未返回 review 结构，不得视为零风险"
elif review.get("status") in ("skipped", "not_run", "error"):
    review_state = str(review.get("status"))
    review_reason = str(review.get("reason") or "")
    review_error = str(review.get("error") or "")
issues = (review or {}).get("issues") if isinstance((review or {}).get("issues"), list) else []
if issues:
    issues_text = "...（按现有逻辑）..."
elif review_state == "executed" and review is not None:
    issues_text = "未发现审查问题或警告。"
else:
    issues_text = f"审查未执行/异常（{review_state}）：{review_reason} {review_error}".strip()
```
并在 return 中加入：
```python
"review_state": review_state,
"review_reason": review_reason,
"review_error": review_error,
```

**3) 验收**：review 缺失时 issues_text 不得是"未发现审查问题或警告"。

---

### A3. 节点 1785836928806「真实接口结果判断」（If-Else）

**当前条件（case true 只有一条）**
```json
{"case_id": "true",
 "conditions": [
   {"comparison_operator": "is", "id": "27157766-1ea6-489a-84e0-60267e0c5e0d",
    "value": true, "varType": "boolean",
    "variable_selector": ["1785836509606", "api_success"]}
 ],
 "id": "true", "logical_operator": "and"}
```
**改为**：在该 case 的 conditions 数组新增第二条（AND）：
```json
{"comparison_operator": "is", "id": "new-review-state-check",
 "value": "executed", "varType": "string",
 "variable_selector": ["1785836509606", "review_state"]}
```
即 true 分支条件 = `api_success == true AND review_state == "executed"`。

---

### A4. 节点 1786031004832「全流程交付报告格式化」（Code）

**1) variables：新增 3 项**

新增项（追加到现有 variables 列表末尾）：
```json
{"value_selector": ["1786378171088", "environment_pending_count"], "value_type": "number", "variable": "environment_pending_count"},
{"value_selector": ["1786378171088", "manual_package_count"], "value_type": "number", "variable": "manual_package_count"},
{"value_selector": ["1785836509606", "review_state"], "value_type": "string", "variable": "review_state"}
```
> 依赖：施工工具 End 必须先输出 environment_pending_count/manual_package_count（见 B 节），否则这里选不到变量。

**2) 函数签名新增参数**
```python
environment_pending_count: int = 0,
manual_package_count: int = 0,
review_state: str = "",
```
并在函数内做 `number()` 转换后加入判定：
```python
pending_count = confirm_count + conflict_count + unknown_count \
                + environment_pending_count + manual_package_count
```

**3) 终态判定当前 → 改为**

当前：
```python
if str(fiber_status) == "失败" or str(construction_status) == "失败":
    overall_status = "流程失败"
elif failed_rules > 0:
    ...
elif pending_count > 0:
    overall_status = "条件通过，需人工确认"
else:
    overall_status = "全流程通过"
```
改为（机器枚举，中文只作展示）：
```python
machine_error = (
    str(fiber_status) in ("失败", "FAILED", "解析失败")
    or str(construction_status) in ("失败", "FAILED", "解析失败")
    or review_state in ("error", "missing")
)
if machine_error:
    overall_status = "BLOCKED"
elif failed_rules > 0:
    overall_status = "CONDITIONAL_PASS_ESTIMATED"
elif pending_count > 0:
    overall_status = "CONDITIONAL_PASS"
else:
    overall_status = "PASS"
```

**4) outputs：新增 2 项**
```json
{"result_type": {"children": null, "type": "string"}},
{"stage_results_json": {"children": null, "type": "string"}}
```
return 中返回：
```python
"result_type": "formal" if overall_status == "PASS" else "estimated_or_conditional",
"stage_results_json": json.dumps({...阶段状态...}, ensure_ascii=False),
```

---

### A5. 节点 1785573827916「交付流程状态输出」（End）

**当前 outputs 第一项**
```json
{"value_selector": ["1786031004832", "final_report"], "value_type": "string", "variable": "delivery_status"}
```
**改为**：保留现有项，并在列表末尾新增：
```json
{"value_selector": ["1786031004832", "overall_status"], "value_type": "string", "variable": "overall_status"},
{"value_selector": ["1786031004832", "result_type"], "value_type": "string", "variable": "result_type"},
{"value_selector": ["1786031004832", "stage_results_json"], "value_type": "string", "variable": "stage_results_json"}
```
建议同时把 `delivery_status` 的 variable 改名 `final_report`（若外部契约要求旧名，则保留 `delivery_status` 仅作展示）。

---

### A6. HTTP 节点（Body/Headers 定位）

**1786366895071 调用BOM_DEV_API**
- URL：`http://118.31.127.213:8001/agent/bom`（保持）
- Headers：`Content-Type:application/json`（保持）
- Body（type=json，当前值）：
```json
{"data": [{"id": "key-value-129", "key": "", "type": "text",
           "value": "{\n  \"project_id\": \"{{#1785836509606.project_id#}}\"\n}"}],
 "type": "json"}
```
- 无需改动；若之后总控需要传 engineering_data，则把 value 改为 `{{#1786367110630.body#}}`。

**1785834889406 HTTP 请求（审查）**
- URL：`http://118.31.127.213:8001/agent/data-pipeline`（保持）
- Headers：空（保持）
- Body：multipart，file=1785836300668.first_record + excel_limit=0/pdf_chars=0/include_tables=false/compact=true（保持）

---

## B. 施工指令工具（PROD）当前节点修改

### B1. 代码节点 1785839947624「代码执行」

**当前 outputs schema 键**：status, construction_status, instruction_count, work_package_count, unknown_count, normal_report, summary, unknown_objects_json, construction_result_json, instructions_json, work_packages

**新增 outputs schema**
```json
{"environment_pending_count": {"children": null, "type": "number"}},
{"manual_package_count": {"children": null, "type": "number"}},
{"pending_review_items_json": {"children": null, "type": "string"}}
```
**代码 return 新增**
```python
"environment_pending_count": environment_pending_count,
"manual_package_count": manual_package_count,
"pending_review_items_json": json.dumps(pending_review_items, ensure_ascii=False),
```

### B2. End 节点 1785841073663「输出」

**当前 outputs**：construction_report, construction_status, instruction_count, work_package_count, unknown_count, construction_result_json, work_packages

**新增 3 项**
```json
{"value_selector": ["1785839947624", "environment_pending_count"], "value_type": "number", "variable": "environment_pending_count"},
{"value_selector": ["1785839947624", "manual_package_count"], "value_type": "number", "variable": "manual_package_count"},
{"value_selector": ["1785839947624", "pending_review_items_json"], "value_type": "string", "variable": "pending_review_items_json"}
```
> 完成此步后，总控 A4 才能引用 `1786378171088.environment_pending_count`。

### B3. material_for_codes 精确修改点（解决 AUD-12）

当前关键行：
```python
related = str(item.get("对应设计对象") or item.get("design_object") or item.get("object_code") or "")
if not any(code and code in related for code in codes): continue
qty = item.get("数量") if "数量" in item else item.get("quantity")
...
return list(grouped.values())[:30]
```
改为：
```python
item_ids = item.get("source_object_ids") or []
hit = bool(set(item_ids) & set(source_ids)) if source_ids else bool(set(codes) & set(item_ids))
if not hit: continue
qty = item.get("quantity")
if qty is None:
    qty = item.get("最终数量")
unit = item.get("unit") or item.get("单位")
material_code = item.get("material_code") or item.get("物料编码")
material_name = item.get("material_name") or item.get("物料名称")
# 去重聚合后完整返回；截断仅用于 displayed_materials
```

### B4. fiber_pending 精确修改点（CON-06）

当前：
```python
fiber_pending = bool(fiber_conflicts or fiber_status == "待确认")
```
改为：
```python
fiber_pending = bool(
    fiber_conflicts
    or fiber_status in ("待确认", "失败", "FAILED", "解析失败")
)
```

---

## C. 纤芯分配工具（PROD）当前节点修改

### C1. 代码节点 1785820320131「纤芯分配引擎」

**当前 outputs schema 键**：fiber_report, fiber_status, fiber_count, splice_count, conflict_count, fiber_result_json

**新增 outputs schema**
```json
{"cable_allocation_row_count": {"children": null, "type": "number"}},
{"splice_location_count": {"children": null, "type": "number"}},
{"splice_core_count": {"children": null, "type": "number"}},
{"retrieval_status": {"children": null, "type": "string"}}
```
**代码 return 新增**
```python
"cable_allocation_row_count": len(splice_table),
"splice_location_count": splice_location_count,   # 有真实接续表才计数，缺省 None/0 注明
"splice_core_count": splice_core_count,           # 逐芯熔接次数，缺省 None/0 注明
"retrieval_status": retrieval_status,             # 检索节点不可用/失败/成功
```
同时按 FIB-01/02/05 修改占用/连续/输入校验逻辑（见《Dify修复清单与修复方式》对应节）。

### C2. End 节点 1785820356858「输出」

**当前 outputs**：fiber_report, fiber_status, fiber_count, splice_count, conflict_count, fiber_result_json

**新增 4 项**
```json
{"value_selector": ["1785820320131", "cable_allocation_row_count"], "value_type": "number", "variable": "cable_allocation_row_count"},
{"value_selector": ["1785820320131", "splice_location_count"], "value_type": "number", "variable": "splice_location_count"},
{"value_selector": ["1785820320131", "splice_core_count"], "value_type": "number", "variable": "splice_core_count"},
{"value_selector": ["1785820320131", "retrieval_status"], "value_type": "string", "variable": "retrieval_status"}
```

---

## D. 修改后的操作顺序

1. 先改施工工具 B1/B2 并发布；
2. 再改总控 A1~A5（此时才可选到 B2 新输出）并发布；
3. 纤芯工具 C1/C2 与总控无关，可并行，发布后重绑 1786378158503；
4. 每次修改后：Dify 内点"发布"，再用 API 调用已发布版本复测，编辑草稿不会生效。

> 本实施单所有节点均来自当前实际 YAML；凡未列出的节点（1786367110630、1786367437101、1786378158503 等）除重绑版本外无需改动。
