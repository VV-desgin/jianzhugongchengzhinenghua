# 纤芯分配工具（PROD）修复文档（2026-09-05）

> 文件：`D:\浏览器下载(安装包)\纤芯分配工具（PROD）.yml`
> 节点：`1785820320131 纤芯分配引擎`、`1785820356858 输出(End)`
> 当前核验结论：
> - ✅ End/输出 schema 已新增 `cable_allocation_row_count/splice_location_count/splice_core_count/retrieval_status`
> - ❌ 引擎代码仍是原版（9101 字符，SHA `f35f491120`），FIB-01/02/04/05 未修复
> - ❌ 代码 return 未返回新增 4 个字段，运行会报"缺少输出"

---

## 一、网页端操作顺序（只改纤芯工具）

1. 打开应用「纤芯分配工具（PROD）」→ 编排；
2. 点击节点 `1785820320131 纤芯分配引擎`；
3. 检查「输出变量」已包含（若无则添加）：
   - cable_allocation_row_count(number)
   - splice_location_count(number)
   - splice_core_count(number)
   - retrieval_status(string)
4. 用下方"完整代码块"替换代码编辑器内容（FIB-02 默认实现**连续选芯**，与文档承诺一致；如需最小可用芯号，把 `ALLOCATION_POLICY` 改为 `"smallest_free"`）；
5. 点击 End 节点 `1785820356858 输出`，确认输出变量新增：
   - cable_allocation_row_count ← 1785820320131.cable_allocation_row_count
   - splice_location_count ← 1785820320131.splice_location_count
   - splice_core_count ← 1785820320131.splice_core_count
   - retrieval_status ← 1785820320131.retrieval_status
6. 保存并发布。

---

## 二、修复项与代码块（替换到 `纤芯分配引擎`）

### 2.1 输入校验（FIB-05）：required_cores 必须正整数

替换原 `requested_cores = max(1, to_int(required_cores, 1))` 所在行：

```python
        if isinstance(required_cores, bool) or required_cores is None:
            raise ValueError("required_cores 必须为正整数")
        try:
            requested_cores = int(required_cores)
        except (TypeError, ValueError):
            raise ValueError("required_cores 必须为正整数")
        if requested_cores < 1:
            raise ValueError("required_cores 必须为正整数")
```

### 2.2 占用一致性（FIB-01）：聚合占用 + 逐芯表 + 重复占用

替换 `occupied = set()` 到分配循环前的占用构造段：

```python
            existing = get_existing(code)
            if capacity <= 0:
                conflicts.append({
                    "冲突类型": "容量缺失或无效",
                    "光缆": code,
                    "详情": f"光缆容量为 {capacity}，无法执行纤芯分配",
                    "严重程度": "ERROR",
                })
                continue

            seen_pos = {}
            for item in existing:
                pos = (
                    to_int(item.get("tube"), 0),
                    to_int(item.get("fiber"), 0),
                    to_int(item.get("core"), 0),
                )
                if pos in seen_pos:
                    conflicts.append({
                        "冲突类型": "重复占用记录",
                        "光缆": code,
                        "详情": f"同一光缆存在重复逐芯占用 {pos}",
                        "严重程度": "ERROR",
                    })
                seen_pos[pos] = True

            occupied = set(seen_pos.keys())

            agg_used = to_int(
                cable.get("nb_fibre_util")
                or cable.get("nb_fibre_d")
                or cable.get("used_fibers"),
                -1,
            )
            if agg_used >= 0 and existing:
                if len(existing) != agg_used:
                    conflicts.append({
                        "冲突类型": "聚合与逐芯占用不一致",
                        "光缆": code,
                        "详情": f"聚合已用 {agg_used} 芯，逐芯表 {len(existing)} 条",
                        "严重程度": "ERROR",
                    })
            if agg_used > 0 and not existing:
                conflicts.append({
                    "冲突类型": "仅聚合占用无逐芯表",
                    "光缆": code,
                    "详情": f"聚合已用 {agg_used} 芯但没有逐芯占用表，禁止猜测空闲",
                    "严重程度": "ERROR",
                })
                continue
            if len(occupied) >= capacity:
                conflicts.append({
                    "冲突类型": "满占用/超容量",
                    "光缆": code,
                    "详情": f"已占用 {len(occupied)} 芯，容量仅 {capacity} 芯，禁止新增分配",
                    "严重程度": "ERROR",
                })
                continue
```

### 2.3 连续选芯（FIB-02）+ 空缆处理

替换原 `for tube in range(...)` 双层扫描分配循环：

```python
            ALLOCATION_POLICY = "contiguous"   # 改为 "smallest_free" 则恢复最小可用策略
            cores_per_tube = max(1, capacity // modulo)
            total_slots = modulo * cores_per_tube
            occupied_nums = set()
            for (tube, fiber_no, core) in occupied:
                occupied_nums.add((tube - 1) * cores_per_tube + core)

            free_nums = [n for n in range(1, total_slots + 1) if n not in occupied_nums]
            new_assignments = []
            chosen_nums = []

            if ALLOCATION_POLICY == "contiguous":
                for i in range(0, len(free_nums) - requested_cores + 1):
                    window = free_nums[i:i + requested_cores]
                    if window[-1] - window[0] == requested_cores - 1:
                        chosen_nums = window
                        break
            else:
                chosen_nums = free_nums[:requested_cores]

            if len(chosen_nums) >= requested_cores:
                for n in chosen_nums:
                    tube = (n - 1) // cores_per_tube + 1
                    core = (n - 1) % cores_per_tube + 1
                    position = (tube, 1, core)
                    row = {
                        "光缆编码": code,
                        "上游设备": upstream,
                        "下游设备": downstream,
                        "管号": tube,
                        "纤号": 1,
                        "芯号": core,
                        "状态": "occupied",
                    }
                    new_assignments.append(row)
                    fiber_table.append(row)
                    occupied.add(position)
            else:
                conflicts.append({
                    "冲突类型": "可用连续纤芯不足",
                    "光缆": code,
                    "详情": f"需要连续 {requested_cores} 芯，实际无连续空闲区间",
                    "严重程度": "ERROR",
                })
```

空 `cables` 处理：在 `target_cables` 为空且无冲突时返回 NOT_APPLICABLE，不返回"正常"：

```python
        if not target_cables:
            fiber_report = "# 纤芯分配结果\n\n- 状态：NOT_APPLICABLE\n- 原因：无光缆对象或未提供纤芯业务，不执行分配\n"
            compact_result = {
                "status": "NOT_APPLICABLE",
                "required_cores": requested_cores,
                "fiber_count": 0,
                "cable_allocation_row_count": 0,
                "splice_location_count": 0,
                "splice_core_count": 0,
                "conflict_count": 0,
                "splice_table": [],
                "conflicts": [],
                "conflict_summary": "",
                "retrieval_status": retrieval_status,
            }
            return {
                "fiber_report": fiber_report,
                "fiber_status": "NOT_APPLICABLE",
                "fiber_count": 0,
                "splice_count": 0,
                "conflict_count": 0,
                "fiber_result_json": json.dumps(compact_result, ensure_ascii=False, separators=(",", ":")),
                "cable_allocation_row_count": 0,
                "splice_location_count": 0,
                "splice_core_count": 0,
                "retrieval_status": retrieval_status,
            }
```

### 2.4 splice_count 拆字段（FIB-04）

在 `compact_result` 与 return 中新增：

```python
        splice_location_count = 0      # 只有真实接续表/逐芯记录存在时才统计
        splice_core_count = 0          # 只有逐芯熔接记录存在时才统计
        retrieval_status = "unused"    # 引擎不使用 kb_result；检索失败/不可用也如实返回

        compact_result = {
            "status": status,
            "required_cores": requested_cores,
            "fiber_count": len(fiber_table),
            "cable_allocation_row_count": len(splice_table),
            "splice_location_count": splice_location_count,
            "splice_core_count": splice_core_count,
            "splice_count": len(splice_table),          # 兼容旧字段，注明为光缆汇总行数
            "conflict_count": len(conflicts),
            "splice_table": splice_table,
            "conflicts": conflicts,
            "conflict_summary": conflict_summary,
            "retrieval_status": retrieval_status,
        }
```

return 中追加：

```python
        "cable_allocation_row_count": len(splice_table),
        "splice_location_count": splice_location_count,
        "splice_core_count": splice_core_count,
        "retrieval_status": retrieval_status,
```

### 2.5 RAG-01：kb_result 不使用/检索状态显式化

- 引擎内不读取 `kb_result` 计算；
- 在函数开头增加：
```python
    retrieval_status = "unused"
    if kb_result is not None:
        try:
            retrieval_status = "available"
        except Exception:
            retrieval_status = "unavailable"
```
- 若知识检索失败/为空，输出 `retrieval_status="unavailable"`，不允许影响确定性分配。

---

## 三、修复后验收用例

| 用例 | 输入 | 期望 |
|---|---|---|
| AUD-07 | 容量 24、聚合已用 24、无逐芯表 | 状态含 ERROR/待确认，不得新增 4 芯 |
| AUD-09（连续策略） | 芯 2 已占用、需求 2 | 分配 3、4（`1-1-3,1-1-4`） |
| FIB-05 | required_cores=-5 | 状态失败，不静默改为 1 |
| 空 cables | 无光缆对象 | NOT_APPLICABLE（带原因），不是"正常" |
| 输出字段 | 正常输入 | 返回 cable_allocation_row_count/splice_location_count/splice_core_count/retrieval_status |

---

## 四、仍未定的决策（不影响上述代码粘贴）

1. FIB-02 选连续选芯还是最小可用芯号（本文档默认连续选芯，代码可切换）；
2. FIB-03 是否新增后端确定性接口、Dify 节点是否改绑（本文档只改独立工具内部）。
