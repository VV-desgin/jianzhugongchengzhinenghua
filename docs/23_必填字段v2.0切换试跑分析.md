# 23 必填字段 v2.0 切换试跑分析（2026-08-22）

> 背景：docs/15 A2/C1 曾列「必填字段对齐 v2.0 / 是否切换全集需确认」。本次以试跑数据定案，不做未经验证的切换。
> 方法：读 `docs/官方固定数据/字段口径配置_v2.0.json` required_fields，与 `layer_mapping.yaml` 顶层 required_fields 做双向差集；分别以「并集（=现状）」与「精确 v2.0」两种配置临时替换 yaml 跑 data-pipeline（赛题一 + TC-01 回归夹具），跑后已还原（git 无残留）。

## 一、差集结论：v2.0 无缺漏，当前配置是超集

- v2.0 → 当前：**0 个字段缺失**（8-20 提交 ffbcf86 已按 v2.0 重建 required_fields）。
- 当前 → v2.0（多出的 21 个必填字段）：

| 图层 | 当前多查的字段 |
|---|---|
| BOITE | ADRESSSE、CODE_POSTAL、NB_LOGEMENT、NB_SPLICES、REF_NRO |
| CABLE | TYPE_FIBRE、TYPE_PROP |
| IMB | CODE_POSTAL、CODE_VOIE、TYPE_BATIMENT、VILLE |
| INFRASTRUCTURE | COMPOSITION、EXTREMITE、ORIGINE |
| PTECH | ADRESSSE、CODE_POSTAL、HAUTEUR_APPUI、NB_BOITIERS、TYPE_APPUI |
| SITE | ADRESSSE、CODE_POSTAL |

## 二、试跑结果（R021 检出数）

| 包 | 现状（并集） | 精确 v2.0 | 变化 |
|---|---|---|---|
| 赛题一.zip | 0 | 0 | 无 |
| TC-01_正确工程案例.xlsx | 97 | 82 | 少查 15 处（ADRESSSE/CODE_POSTAL/TYPE_FIBRE/TYPE_PROP/HAUTEUR_APPUI/NB_BOITIERS/TYPE_APPUI/COMPOSITION/ORIGINE/EXTREMITE/CODE_VOIE/TYPE_BATIMENT/VILLE 等） |

> 赛题一在两种口径下均为 0（其数据字段齐全）；差异只体现在精简案例 TC-01 上。

## 三、结论与建议

1. **「补齐 v2.0 必填缺口」已完成**（0 缺口），无需新增字段。
2. **不建议切到精确 v2.0**：那会移除 21 个现有必填检查，TC-01 少检出 15 处「字段缺失」——属于放宽口径；官方 v2.0 是底线而非上限，移除这些检查会让真实缺字段漏检（ADRESSSE/CODE_POSTAL 等仍是官方字段说明里的有效字段）。
3. 若未来组委会/官方要求「与 v2.0 逐字一致」，按本报告第二节数据再决策（改动 = 删 21 个字段 + TC-01 基线 109/8/101 → 94/8/86 + 回归门更新），属口径决策而非确定性改动。
4. 本试跑不改代码、不改基线；layer_mapping.yaml 已还原，pytest/回归门不受影响。
