# 07 业务参数与规则路由说明

> 本地交付文档。业务参数默认值依据行业标准惯例设定，D01~D07 已于 2026-08-22 全部落定
> （记录见 `business_params.json` 的 `_meta.official_decisions`）。更新时间：2026-08-23。

## 1. 业务参数（business_params）

唯一事实源：`design_parser/mappings/business_params.json`；
加载器：`design_parser/business_params.py::load_business_params()`（文件缺失/损坏时回退内置默认值）。

| 参数组 | 键 | 默认值（2026-08-22 官方口径已落定） |
|---|---|---|
| 损耗率 | `loss_rates` | 光缆 500002050=5%、钢绞线 200001033=3%、电杆 poles=0%（备件 2%）、结构件 500002054/500002142=1%、易损件 200000185 等=2~3% |
| 预留长度 | `reserve_lengths` | PCP 盒内余长 1.35m、接头每侧 7.5m、人孔 0.75m、电杆上下杆 7.5m、端点 2.0m、局站地下 7.5m/地上 12.5m、弯曲增长直埋 7‰/管道 10‰/架空 10‰ |
| 包装取整 | `packaging` | 光缆 2km/盘向上取整；钢绞线 500m/卷；熔接点按实际（exact）；电杆/分路器等按整数向上取整 |
| 利旧 | `reuse` | 标记字段 `reuse`；高可能利旧物料 500002480/500002337/500002159/500004729；钢绞线 200001033 需人工确认；默认 `new_with_notice` |
| 纤芯预留策略 | `fiber_policy` | required_cores 默认 4；每 PCP 熔接 4 芯；贯通熔接 true；停放区端面保护、不计熔接量 |
| 施工指令颗粒度 | `instruction_levels` / `process_cards` | 两级：PCP + 工序（PCP安装/光缆成端与熔接/分光器安装/光路测试） |

- `GET /agent/business-params`：返回上述参数 JSON，供 Dify 工具节点消费。
- BOM 公式（`design_parser/bom_formula.py::compute_bom_quantity`）：
  净量 → 损耗 → 预留（米，KM 单位折千） → 包装取整，输出 `net/loss/reserve/before_pack/final/unit/detail/source`。
- `engineering_data` 对象新增可选 `reuse` 字段（`yes/no`，`_normalize_reuse` 归一化 OUI/YES/1/TRUE/利旧）。
- 核心计算字段零值拦截（2026-08-23，评测 TC-14）：CABLE/INFRASTRUCTURE.LONGUEUR 为 0.0/负值时 R005 报「长度缺失」；BOM 对零长度光缆标「待人工确认」，不再静默计入。

## 2. 规则路由：R005_1 已按官方口径启用（2026-08-16 确认）

- **官方规则**：规则库 v2.0 `R-REL-001`（图层表字段说明 校验规则表_行 5.1）——
  提取 SITE(TYPE=PM) 的 CODE 与 ZPM 的 CODE 双向比对，标准值 `SITE.CODE = ZPM.CODE` 一一对应。
- **实现**：`rule_engine.py::check_site_pm_zpm_bidirectional` 与官方文本一致（见 `tests/test_rule_r005_1_official_behavior.py`）。
- **口径**：按官方源文件口径（方案 B），R005_1 已接入四个 GIS 类目路由。
- **口径影响**：使用 `ZPM.CODE=ZPM-01` + `ZPM.REF_PM=SITE.CODE` 编码模型的包（如 TC-01 2.6、测试方包 TC-09/TC-10 系列）
  按官方口径将报 R005_1 error×2——这是官方规则下的合规判定，不是误报；如需此类包通过，需按官方模型
  修正包内 ZPM.CODE=SITE.CODE（官方赛题一/四本身即 CODE 相等模型，不受影响）。
- **已路由**：R005_1、R005_3（CABLE.REF_PM↔SITE.CODE）、R027（PM 在 ZPM 范围内）、R028（PBO 在 ZPM 范围内）、
  R033（8 个官方图层非空）。赛题一/四基线保持 285/57/228（R005_1 在 CODE 相等模型下贡献 0 条）。

## 3. 基线（本地实测；最新口径见 docs/19）

| 案例 | 结果 |
|---|---|
| 场勘设计图.zip | 43/43/0 |
| 赛题一.zip / 赛题四-摩洛哥.rar | 287/57/230（+R-GIS-004×2，规则分布精确） |
| TC-01 2.6/2.8 | 57/57/0（R005_1 双口径后全绿） |
| TC-09 仅占用/仅重复 | 59/57/1（R-FIBER-002 warning + R021） |
| TC-10 字段为空 | 65/57/8（R005/R005_1~R005_3/R005_4/R021） |
| TC-11 未收录物料（2.0） | 61/57/3（R-BOM-001 + R005_4×2 + R032；2026-08-23 非标箱体类型拦截） |
| TC-12 利旧冲减（2.0） | 57/57/0；BOM 利旧冲减 2 条光缆（STATUT=REUSE，2026-08-23） |
| TC-14 长度缺失（2.0） | 58/57/1（R005 零值拦截，2026-08-23） |

## 4. 口径落定（2026-08-22）

D01~D07 已按官方答复全部落定：D04=reuse 字段/承载力归设计院/只减新购不删工序；D06=计划工期暂不要求；其余按现状定稿。
记录见 `business_params.json` 的 `_meta.official_decisions`（official_pending 已清空）。

## 5. R005_1 双口径说明（2026-08-22 优化）

- **口径**：SITE(TYPE=PM) 与 ZPM 双向覆盖检查，允许两种关联写法任一满足即通过：① 同码模型 `ZPM.CODE == SITE.CODE`（官方 5.1 原文）；② 外键模型 `ZPM.REF_PM == SITE.CODE`（正确工程包建模）。
- **依据**：官方《图层表字段说明和数据校验规则.xlsx》6.4/6.5 明确 `BOITE/CABLE.REF_PM = SITE.CODE = ZPM.CODE`，REF_PM 与同码在官方体系内等价；双口径使 R005_1 与 R005_2/R005_3（本就按 REF_PM）口径统一。
- **语义不变**：仍双向校验——每个 PM 必须被至少一个 ZPM 覆盖（同码或 REF_PM），每个 ZPM 必须归属真实存在的 PM；孤立/引用缺失照常检出。
- **实测**：TC-01 2.8 由 59/57/2 → **57/57/0 全绿**（Dify 全流程 succeeded）；赛题一/四基线不变（287/57/230）；测试方 TC-04 孤立 64/57/7 → 62/57/5、TC-10 字段空 65/57/8 不变（真实孤立仍检出）。
- **可回退**：若组委会要求严格按 5.1 原文同码判定，可一行开关切回（配置化或代码回退），见 tests/test_rule_r005_1_official_behavior.py 双口径用例。
