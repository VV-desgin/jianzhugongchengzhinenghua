# Dify 修改执行顺序（2026-09-05）

> 规则：**施工工具先发布 → 总控再改**（总控要引用施工工具的新输出）；总控内部按"信号源 → 判断 → 汇总 → 出口"顺序；纤芯工具独立可并行。
> 每完成一步并发布后，再进入下一步；未发布前不要修改依赖它的下游节点。

---

## 第 0 步：前置确认（无需改）

- [ ] 后端已部署：`118.31.127.213:8001/agent/bom` 返回 `quantity/unit/material_code/source_object_ids`；
- [ ] 后端已部署：`/agent/data-pipeline` 返回 `review.status` 与 `stage_results`；
- [ ] 打开三个应用：施工指令工具（PROD）、通信工程智能化总控（PROD）V1.0！！、纤芯分配工具（PROD）。

---

## 第 1 步：先改「施工指令工具（PROD）」

### 1.1 节点「代码执行」（1785839947624）

1. 右侧"输出变量"新增 3 个：
   - `environment_pending_count`（number）
   - `manual_package_count`（number）
   - `pending_review_items_json`（string）
2. 代码页按顺序替换（使用完整代码包 `04_construction_fix_snippets_FULL.py`）：
   - 替换位置 1：fiber_pending（CON-06）
   - 替换位置 2：object_records 构建段（CON-02）
   - 替换位置 3：material_for_codes 完整函数（CON-03/CON-04）
   - 替换位置 4：PCP_OFFICIAL_EVIDENCE（CON-05）
   - 替换位置 5：procedure_ref（CON-05）
   - 替换位置 6：最外层 return 追加 3 个字段
3. 保存。

### 1.2 节点「输出」（End，1785841073663）

右侧"输出变量"新增 3 个：

| 输出变量 | 类型 | 来源 |
|---|---|---|
| environment_pending_count | number | 代码执行 → environment_pending_count |
| manual_package_count | number | 代码执行 → manual_package_count |
| pending_review_items_json | string | 代码执行 → pending_review_items_json |

### 1.3 发布

点击「发布」。发布后再进第 2 步。

> ⛔ 阻塞点：总控第 2.4 步要选择 `1786378171088.environment_pending_count`，如果这里没发布，总控里选不到。

---

## 第 2 步：修改「通信工程智能化总控（PROD）V1.0！！」

### 2.1 节点「解析黄福林BOM结果」（1786023921854）— 第 1 个改

- 输入变量新增：`api_success`（boolean，来源=解析BOM_DEV_API返回.api_success）、`api_error`（string，来源=.api_error）；
- 代码整节点替换为 `docs/dify_fix_code/01_parse_BOM_result_FULL.py`；
- 保存（不发布，等第 2.5 一起发布）。

### 2.2 节点「解析真实接口返回」（1785836509606）— 第 2 个改

- 输出变量新增：`review_state`、`review_reason`、`review_error`（均为 string）；
- 代码整节点替换为 `docs/dify_fix_code/02_parse_review_api_FULL.py`；
- 保存。

### 2.3 节点「真实接口结果判断」（1785836928806，If-Else）— 第 3 个改

- 依赖 2.2 的 `review_state`；
- true 分支新增条件：`review_state == "executed"`（string，等于 executed）；
- 原条件 `api_success == true` 保留，逻辑 AND；
- 保存。

### 2.4 节点「全流程交付报告格式化」（1786031004832）— 第 4 个改

- 依赖 2.1、2.2，以及第 1 步施工工具发布；
- 输入变量新增：
  - `environment_pending_count`（number，来源=施工工具 1786378171088.environment_pending_count）
  - `manual_package_count`（number，来源=1786378171088.manual_package_count）
  - `review_state`（string，来源=1785836509606.review_state）
- 输出变量新增：`result_type`（string）、`stage_results_json`（string）；
- 代码整节点替换为 `docs/dify_fix_code/03_final_report_formatter_FULL.py`；
- 保存。

### 2.5 End 节点「交付流程状态输出」（1785573827916）— 第 5 个改

- 依赖 2.4；
- 输出变量新增：
  - `overall_status` ← 1786031004832.overall_status
  - `result_type` ← 1786031004832.result_type
  - `stage_results_json` ← 1786031004832.stage_results_json
- 保存。

### 2.6 检查 HTTP 节点（不改）

- 1786366895071 调用BOM_DEV_API：URL/Headers/Body 保持 project_id 模式；
- 1785834889406 HTTP 请求：URL/Headers/Body 保持；
- 确认无改动后，进入发布。

### 2.7 发布总控

点击「发布」。发布后做第 4 步冒烟。

---

## 第 3 步（可与第 2 步并行）：修改「纤芯分配工具（PROD）」

> 若 FIB-02（连续选芯）策略未定，可先只加输出字段，算法策略后补。

### 3.1 节点「纤芯分配引擎」（1785820320131）

- 输出变量新增 4 个：
  - `cable_allocation_row_count`（number）
  - `splice_location_count`（number）
  - `splice_core_count`（number）
  - `retrieval_status`（string）
- 代码 return 增加对应 4 个字段（FIB 逻辑按策略另行替换）。

### 3.2 节点「输出」（End，1785820356858）

- 输出变量新增 4 个，来源选 1785820320131 对应输出。

### 3.3 发布纤芯工具

发布后，若版本变化，回总控重绑工具节点（见 2.7 前或之后均可，重绑后需再次发布总控）。

---

## 第 4 步：验证顺序（每改一步建议先验再往下）

1. 施工工具发布后：直接运行施工工具，确认输出含 `environment_pending_count/manual_package_count/pending_review_items_json`；
2. 总控发布后：跑一次 TC-01，确认 outputs 含 `overall_status/result_type/stage_results_json`，且 `review_state=executed`；
3. AUD-12 验收：拿总控返回的 `bom_items` 跑施工，确认材料行有 数量+单位，不再"详见BOM"；
4. 四条路径回归：正向、停止整改、继续预估、仅审查。

---

## 一句话顺序

施工工具(代码执行→End→发布) → 总控(BOM解析→真实接口解析→If-Else→报告格式化→End→发布) → 纤芯(引擎→End→发布→总控重绑→再发布) → 验证。
