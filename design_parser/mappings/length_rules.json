{
  "enabled": true,
  "rules": {
    "CABLE_AMONT": {
      "limit": 20,
      "prefix_mode": "auto",
      "strip_increment": true,
      "note": "临时口径（官方确认前，按团队暂定）：<类型>-<运营商>-（如 CDI-UNF-，8 位）为固定前缀不计入；末尾 -NNN（Increment）为序号/端口不计入；仅设备标识部分计长度（实际最长 17 ≤ 20）。官方字段语义为“上游/入箱光缆编号”；待数据方确认后调整：若 20 为全串硬上限，将 enabled 设为 false 即可恢复全串校验。"
    }
  }
}
