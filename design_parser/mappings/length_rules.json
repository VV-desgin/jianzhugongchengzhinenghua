{
  "enabled": true,
  "rules": {
    "CABLE_AMONT": {
      "limit": 20,
      "prefix_mode": "auto",
      "strip_increment": true,
      "note": "临时口径（Increment 语义已确认）：<类型>-<运营商>-（如 CDI-UNF-，8 位）为固定前缀不计入；末尾 -NNN（Increment）已确认为光缆内部纤芯号/端口号，不计入编号长度；仅编号主体计长度（实际最长 17 ≤ 20）。官方字段语义为“上游/入箱光缆编号”；待数据方最终确认 20 的计法：若 20 为全串硬上限，将 enabled 设为 false 即可恢复全串校验。"
    }
  }
}
