# 代码上传守则（AGENTS.md）

本仓库接收后端代码、必要测试、运行依赖与六份正式交付物文档。
任何 Agent 在提交/推送前必须执行以下守则。

## 一、禁止内容

- 团队成员真实姓名（包括分工、负责人、编制人等栏位中的人名）。
- 内部角色自称词（例如本地记忆中的角色署名用语）。
- 密钥、Token、API Key、密码、服务器口令。
- 本地运行日志、记忆/交接/验收中间记录、评测 JSON、图片证据、临时文件路径。

完整禁止名单与扫描命令只保存在本地守则文件中，不得写入本仓库。

## 二、允许上传范围

允许上传：

- 后端源码：`api.py`、`schemas.py`、`design_parser/**`
- 构建配置：`requirements.txt`、`setup.py`
- 必要测试：`tests/**`
- 运行依赖：`bin/UnRAR.exe` 及其第三方许可 `license.txt`
- 无敏感内容的仓库治理文件：`.gitignore`、`AGENTS.md`
- 正式交付物文档：`docs/` 下仅 `00_作品系统说明.md`、`01_官方规则后端实现覆盖矩阵.md`、
  `03_engineering_data字段说明.md`、`04_FastAPI接口说明.md`、`05_GIS规则测试记录.md`、
  `06_已知限制清单.md` 六份，并以根目录 `README.md` 作为交付索引

不允许上传：

- `docs/` 下除上述六份之外的任何文件（Dify 修复/发布说明、验收记录、过程文档等）
- `EVALUATION_REPORT.md`、`team-memory.md` 等记忆/日志类文件
- `*.zip`、`*.docx`、`*.png`、运行结果 JSON、备份目录

## 三、提交前检查

在仓库根目录用本地守则中的禁止名单执行全文扫描；命中任一项即中止提交。
推送到 GitHub 前，用 `git status` 确认暂存区不包含上述“不允许上传”文件。

## 四、例外处理

若确需在代码注释/测试/文档中引用节点名称，只允许使用中性名称（如“BOM 解析节点”），
不得使用个人姓名或带个人署名的旧节点名。

## 五、外部数据与测试

- 官方固定数据（施工规程知识库 v2.0、设计对象-物料-工序映射表）不随仓库上传；相关测试在缺少数据时必须 skip，不得改用本地过程材料绕过。
- Dify 节点代码、发布包、验收记录、评测 JSON、图片证据不随仓库上传。
- 业务参数快照以 `design_parser/mappings/business_params_snapshot.json` 随后端代码入库，用于 P1-06 参数口径核对。
- 本地复现施工知识库测试时，用环境变量 `DESIGN_PARSER_FIXED_DATA_DIR` 指向本地官方数据目录。
